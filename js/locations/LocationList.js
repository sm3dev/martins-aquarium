/**
 *  LocationList renders individual fish harvest locatios as HTML
 */

 import { Location } from "../locations/Location";

 export const getLocations = getLocation();
addLocationsToDOM(getLocations);